<div class="artistTile">
  {{#link-to 'artist' artist.id}}<h4>{{artist.name}}</h4>{{/link-to}}
  <img src="{{artist.image}}">
  <p>Genre: {{artist.genre}}</p>
  <p>Bio: {{artist.bio}}</p>
  <p>Website: <a href="{{artist.website}}">{{artist.name}}.com</a></p>
</div>
