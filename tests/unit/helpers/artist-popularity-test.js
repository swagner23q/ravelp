import { artistPopularity } from 'ravelp/helpers/artist-popularity';
import { module, test } from 'qunit';

module('Unit | Helper | artist popularity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = artistPopularity([42]);
  assert.ok(result);
});
